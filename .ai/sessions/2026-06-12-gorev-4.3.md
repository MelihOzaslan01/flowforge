# Session Sablonu - dosya adi: YYYY-MM-DD-gorev-X.Y.md

## Oturum Bilgisi
- **Tarih:** 2026-06-12
- **Ajan:** Codex
- **Hedef gorev(ler):** 4.3 - Pod kill failover ve HPA load scriptleri

## Insanin Verdigi Gorev Tanimi
"Once .ai/PROGRESS.md'yi ve son session dosyasini oku. SADECE Gorev 4.3: (1) scripts/chaos-pod-kill.sh: normal job'i tetikler, runId alir, step 2 kosarken (start'tan ~8-10 sn sonra) o adimi isleyen worker pod'unu bulup kubectl delete pod ile oldurur, ardindan runId'yi poll eder; run Completed (veya zombi yolundan Failed+compensated) bitince timeline'i (job_step_runs) basar ve exit code'la sonuclanir. Pod'u bulma: worker_db'den ilgili adimin worker_id'si (hostname=pod adi). (2) scripts/hpa-load.sh: chaos job'ini hizli araliklarla N kez tetikleyip (orn. 30 run) kubectl get hpa -w ile izlemeye uygun yuk uretir. (3) README-k8s.md'ye 'Failover demo' ve 'Watching HPA scale' bolumleri — komutlar + beklenen cikti. (4) Scriptler LF, bash; Windows notu ekle. Dogrulama: build+test + script'lerin shellcheck-vari goz kontrolu (canli demo insan tarafindan kosulacak). Takip + commit (task 4.3)."

## Oturum Sonu Ozeti (ajan doldurur)
- **Tamamlanan:** `scripts/chaos-pod-kill.sh` eklendi. Script normal job'i baslatir, runId alir, step 2 `Running` satirinin `worker_id` degerini `worker_db` uzerinden bulur, pod'u `kubectl delete pod --wait=false` ile siler, run terminal olana kadar poll eder ve `job_step_runs` timeline'ini basar.
- **Tamamlanan:** `scripts/hpa-load.sh` eklendi. Varsayilan olarak `monthly-sales-report-chaos` job'ini 30 kez hizli aralikla tetikler ve HPA/pod watcher komutlarini yazdirir. `k8s/README-k8s.md` icine Failover Demo, Watching HPA Scale ve Windows Bash/LF notlari eklendi.
- **Dogrulama:** `dotnet build .\flowforge.sln -warnaserror` yesil; `dotnet test .\flowforge.sln --no-build` yesil, 7 unit + 3 integration test gecti. `git diff --check` yesil; scriptler LF (`CRLF=0`) ve Git Bash `bash -n` parse kontrolu yesil. Canli k3d pod kill/HPA demosu insan tarafindan kosulacak.
- **Not/risk:** Failover script'i iki beklenen terminal yolu kabul eder: run `Completed` veya zombi temizleyici yolu ile `Failed` + step 1 `Compensated`.
- **Yarim kalan / sonraki oturuma devir:** Faz 4 kapanis gorevi sirada.
- **Bir sonraki oturumun yapmasi gereken ILK sey:** Once `.ai/PROGRESS.md` ve bu session dosyasini oku; ardindan sadece istenen yeni goreve gec.
