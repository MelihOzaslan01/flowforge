# Session Şablonu — dosya adı: YYYY-MM-DD-gorev-X.Y.md

## Oturum Bilgisi
- **Tarih:** 2026-06-10
- **Ajan:** Codex
- **Hedef görev(ler):** 1.5 — OutboxPublisher

## İnsanın Verdiği Görev Tanımı
"Önce .ai/PROGRESS.md'yi ve son session dosyasını oku. Sonra SADECE Görev 1.5'i yap: FlowForge.Outbox classlib + OutboxPublisher. Kurallar: (1) Tasarım §5.4 kalıbı — 500ms PeriodicTimer, batch 100, occurred_at sırasıyla. (2) ProducerConfig: Acks.All + EnableIdempotence=true + MessageSendMaxRetries=5. (3) Kafka mesajının Key'i = aggregate_id, Value'su = payload kolonu OLDUĞU GİBİ (yeniden serileştirme yok — zarf zaten hazır). (4) Publish başarısızsa kayıt SİLİNMEZ, attempt_count++ yapılır ve sonraki turda yeniden denenir; exception loglanır, servis ayakta kalır. (5) ControlPlane'e hosted service olarak kaydedilir. Doğrulama + BACKLOG/PROGRESS + commit no."

## Oturum Sonu Özeti (ajan doldurur)
- **Tamamlanan:** `FlowForge.Outbox` classlib eklendi; shared `OutboxMessage`, `IOutboxDbContext`, `OutboxPublisher`, options ve DI registration oluşturuldu. ControlPlane shared outbox entity'sine geçirildi ve hosted service olarak `AddOutboxPublisher<ControlPlaneDbContext>` kaydedildi.
- **Yarım kalan / sonraki oturuma devir:** Faz 1.6 Worker henüz başlamadı.
- **Bir sonraki oturumun yapması gereken İLK şey:** Önce `.ai/PROGRESS.md` ve bu session dosyasını oku; ardından Görev 1.6 için worker_db, consumer loop, StepExecutor ve inbox/outbox transaction sıralamasına geç.
