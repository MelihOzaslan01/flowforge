# Session Şablonu — dosya adı: YYYY-MM-DD-gorev-X.Y.md

## Oturum Bilgisi
- **Tarih:** 2026-06-10
- **Ajan:** Codex
- **Hedef görev(ler):** 1.6 — Worker

## İnsanın Verdiği Görev Tanımı
Önce .ai/PROGRESS.md'yi ve son session dosyasını oku. Sonra SADECE Görev 1.6'yı yap: FlowForge.Worker. Kurallar: (1) worker_db için kendi DbContext + Initial migration (tasarım §6.2: job_step_runs + outbox_messages + processed_messages; outbox/inbox entity'leri FlowForge.Outbox'tan gelir). (2) Consumer: tasarım §4.3 config'i birebir — GroupId=flowforge-workers, EnableAutoCommit=false, CooperativeSticky. (3) Mesaj işleme sırası MUTLAK: BeginTransaction → inbox kontrolü (varsa Commit offset + return) → StepExecutor.RunAsync → job_step_runs yaz → processed_messages ekle → sonraki saga eventini outbox'a yaz → TX Commit → EN SON consumer.Commit. Offset commit'i transaction'ın İÇİNE ALMA. (4) StepExecutor: step_type'a göre Task.Delay simülasyonu (5s/8s/4s/1s, tasarım §2.1); Faz 1'de chaos/retry YOK. (5) Sonraki event mantığı: stepNo < toplam adım → StepCompleted(n) yayınla ve adım listesini eventte taşı; son adım → JobRunCompleted. (6) Worker da OutboxPublisher<WorkerDbContext> host eder. Rapora consumer loop'un başarı yolundaki kod bloğunu AYNEN yapıştır. Doğrulama + takip + commit no.

## Oturum Sonu Özeti (ajan doldurur)
- **Tamamlanan:** Worker için `WorkerDbContext`, `Initial` migration, `job_step_runs`, shared outbox/inbox entity kullanımı, Kafka consumer loop, `StepExecutor`, saga next-event üretimi ve `OutboxPublisher<WorkerDbContext>` registration eklendi.
- **Yarım kalan / sonraki oturuma devir:** Faz 1.7 ControlPlane projection consumer henüz başlamadı.
- **Bir sonraki oturumun yapması gereken İLK şey:** Önce `.ai/PROGRESS.md` ve bu session dosyasını oku; ardından Görev 1.7 için `controlplane-projection` consumer'ını ekle.
