# Session Şablonu — dosya adı: YYYY-MM-DD-gorev-X.Y.md

## Oturum Bilgisi
- **Tarih:** 2026-06-11
- **Ajan:** Codex
- **Hedef görev(ler):** 2.3 — DLQ yönlendirmesi + StepFailed eventi

## İnsanın Verdiği Görev Tanımı
"Önce .ai/PROGRESS.md'yi ve son session dosyasını oku. SADECE Görev 2.3: retry tükendiğinde (1) orijinal mesaj + hata metadata'sı (exception, attempts, workerId, occurredAt) flowforge.job.events.dlq topic'ine gönderilir, (2) ardından StepFailed eventi outbox'a yazılır, (3) offset commit edilir — mesaj artık yeniden teslim EDİLMEZ (DLQ'ya devredildi). DLQ gönderimi de kayıpsız olmalı: outbox_messages tablosuna nullable topic kolonu ekle (migration, iki DB), OutboxPublisher mesajı bu kolondaki topic'e bassın (null = flowforge.job.events). Böylece DLQ kopyası ve StepFailed AYNI transaction'da güvence altına alınır. Bu şema değişikliğini DECISIONS.md'ye işle (Karar: outbox'a topic kolonu; Alternatif: DLQ'ya direkt produce — crash'te DLQ kaydı kaybolabilirdi, reddedildi). StepFailed'ın compensation tetiklemesi 2.4'te — şimdilik StepFailed yayınlanır, kimse tüketmez (worker'da ResolveWork null yolu zaten geçer, projeksiyon JobRunFailed değil StepFailed olduğu için dokunmaz). Doğrulama: geçici olarak bir teste özel exception fırlatan koşulla DEĞİL — unit test + kod kontrolüyle; canlı DLQ testi 2.5'te chaos flag'le yapılacak. Takip + commit no."

Ek netleştirme: "DLQ outbox kaydının aggregate_id'si runId olacak,fail-fast break davranışı topic'ten bağımsız geçerli."

## Oturum Sonu Özeti (ajan doldurur)
- **Tamamlanan:** Shared outbox entity'sine nullable `Topic` eklendi; ControlPlane ve Worker için `AddOutboxTopic` migration'ları üretildi. `OutboxPublisher` `message.Topic ?? options.Value.Topic` ile publish eder hale geldi ve mevcut fail-fast break davranışı değişmeden kaldı.
- **Tamamlanan:** Worker retry exhausted yolunda DLQ outbox kaydı, `StepFailed` outbox kaydı ve `processed_messages` aynı transaction'a alındı; transaction commit edildikten sonra Kafka offset commit ediliyor. DLQ payload'ı original message + `exception`, `attempts`, `workerId`, `occurredAt` taşıyor ve DLQ outbox `aggregate_id` değeri `runId`.
- **Yarım kalan / sonraki oturuma devir:** `StepFailed` compensation zincirini henüz kimse tüketmiyor; bu 2.4 kapsamında yapılacak. Canlı DLQ testi talimat gereği yapılmadı; chaos flag ile 2.5'te doğrulanacak.
- **Bir sonraki oturumun yapması gereken İLK şey:** Önce `.ai/PROGRESS.md` ve bu session dosyasını oku; ardından sadece Görev 2.4 saga compensation zincirine geç.
