# Session Şablonu — dosya adı: YYYY-MM-DD-gorev-X.Y.md

## Oturum Bilgisi
- **Tarih:** 2026-06-11
- **Ajan:** Codex
- **Hedef görev(ler):** 2.4 — Saga compensation zinciri

## İnsanın Verdiği Görev Tanımı
"Önce .ai/PROGRESS.md'yi ve son session dosyasını oku. SADECE Görev 2.4: compensation zinciri. Kurallar: (1) Worker artık StepFailed'ı tüketir: failedStep=N ise CompensateStep(N-1) eventini outbox'a yazar; N=1 ise telafi edilecek adım yok, doğrudan JobRunFailed yazar. (2) Worker CompensateStep(k)'yı tüketir: StepExecutor.CompensateAsync(k) çalıştırır (step_type başına simülasyon, ~1-2 sn Task.Delay), job_step_runs'a status=Compensated satırı ekler, StepCompensated(k) yayınlar. (3) StepCompensated(k) tüketimi: k>1 ise CompensateStep(k-1), k=1 ise JobRunFailed (payload'da failedStep=N taşınmaya devam eder). (4) Tüm bu işlemeler 1.6'daki AYNI kalıbı kullanır: inbox ön-kontrol → iş → TX(yaz+inbox+outbox) → commit → offset. (5) CompensateStep/StepCompensated payload'ları steps[] ve failedStep'i taşımaya devam eder (D-002 deseni). (6) Compensation adımlarında retry YOK — telafi başarısız olursa WARN logla ve zinciri sürdür (best-effort; bu kararı DECISIONS'a yaz: telafi-retry'ı Faz 2 kapsamı dışı). (7) Projeksiyon değişmez — JobRunFailed'ı 1.7'den beri biliyor. Unit test: 'failedStep=3 → üretilen event dizisi CompensateStep(2), StepCompensated(2), CompensateStep(1), StepCompensated(1), JobRunFailed' zincir mantığı testi. Doğrulama + takip + commit no"

## Oturum Sonu Özeti (ajan doldurur)
- **Tamamlanan:** `StepFailed`, `CompensateStep` ve `StepCompensated` payload'ları stateless compensation için `steps[]`/`failedStep` bağlamını taşıyacak şekilde genişletildi. Worker consumer'ı bu eventleri tüketip ters sıra compensation zincirini outbox üzerinden ilerletiyor.
- **Tamamlanan:** `StepExecutor.CompensateAsync` eklendi; compensation hataları retry edilmeden WARN loglanıyor ve zincir `StepCompensated` ile devam ediyor. Bu best-effort karar D-004 olarak `DECISIONS.md` dosyasına işlendi.
- **Yarım kalan / sonraki oturuma devir:** Canlı chaos/compensation testi yapılmadı; 2.5 chaos flag ve `monthly-sales-report-chaos` seed job'ı ile uçtan uca doğrulanacak.
- **Bir sonraki oturumun yapması gereken İLK şey:** Önce `.ai/PROGRESS.md` ve bu session dosyasını oku; ardından sadece Görev 2.5 chaos flag + chaos seed job'a geç.
