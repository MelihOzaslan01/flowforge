# Session Şablonu — dosya adı: YYYY-MM-DD-gorev-X.Y.md

## Oturum Bilgisi
- **Tarih:** 2026-06-10
- **Ajan:** Codex
- **Hedef görev(ler):** 2.2 — Polly retry

## İnsanın Verdiği Görev Tanımı
"Önce .ai/PROGRESS.md'yi ve son session dosyasını oku. SADECE Görev 2.2: StepExecutor.RunAsync çağrısını Polly ResiliencePipeline ile sar — exponential backoff 2s/4s/8s, deneme sayısı eventteki step.maxRetries'tan. Her başarısız deneme job_step_runs'a AttemptCount artışıyla işlenir ve WARN loglanır (runId, stepNo, attempt, hata). Retry'lar TX İÇİNDE DEĞİL — adım denemeleri transaction başlamadan önce biter; TX yalnızca başarılı sonucu yazar. Retry tükenirse şimdilik exception fırlat (mesaj yeniden teslim edilir — DLQ 2.3'te). Chaos flag'e DOKUNMA (2.5'te). Doğrulama + takip + commit no."

## Oturum Sonu Özeti (ajan doldurur)
- **Tamamlanan:** Worker `StepExecutor.RunAsync` çağrısı Polly `ResiliencePipeline` retry sarmalına alındı. Başarısız denemeler transaction dışında `job_step_runs` tablosuna `Failed` olarak, artan `AttemptCount` ile yazılıyor ve WARN loglanıyor; başarılı sonuç/inbox/outbox transaction'ı retry tamamlandıktan sonra açılıyor.
- **Yarım kalan / sonraki oturuma devir:** Retry tükenince exception fırlatma davranışı bilinçli bırakıldı; DLQ yönlendirmesi 2.3'te yapılacak.
- **Bir sonraki oturumun yapması gereken İLK şey:** Önce `.ai/PROGRESS.md` ve bu session dosyasını oku; ardından sadece Görev 2.3 DLQ yönlendirmesine geç.
