# Session Şablonu — dosya adı: YYYY-MM-DD-gorev-X.Y.md

## Oturum Bilgisi
- **Tarih:** 2026-06-10
- **Ajan:** Codex
- **Hedef görev(ler):** 1.8 — Docker compose + smoke

## İnsanın Verdiği Görev Tanımı
"Önce .ai/PROGRESS.md'yi ve son session dosyasını oku. SADECE Görev 1.8: (1) Her servis için Dockerfile (multi-stage: sdk build → aspnet/runtime). (2) docker-compose.yml tasarım §11 baz alınarak: kafka (KRaft, tek broker), kafka-ui, postgres (init-dbs.sql ile control_db + worker_db), controlplane (5000:8080), worker (Faz 1'de replicas:1), volumes. (3) kafka-init one-shot container: 3 topic'i tasarım §4.1 partition sayılarıyla yaratır (--if-not-exists). (4) KRİTİK — connection string'ler compose network adlarıyla: Kafka=kafka:9092, Postgres Host=postgres; bunlar environment variable ile override edilir, appsettings'teki localhost değerleri lokal IDE çalışması için kalır. (5) Servisler Kafka/DB hazır olmadan açılabilir: startup'ta bağlantı retry'ı olmalı (basit bekleme döngüsü yeterli), çökmek yasak; compose'da depends_on + postgres healthcheck ekle. (6) scripts/smoke.sh: POST /run → 60 sn boyunca GET /runs/{id} poll → Completed ise exit 0, değilse logları basıp exit 1. LF satır sonlu olmalı. (7) Doğrulama: docker compose up -d --build → ./scripts/smoke.sh yeşil → outbox lag sorgusu 0. Takip + commit no."

## Oturum Sonu Özeti (ajan doldurur)
- **Tamamlanan:** ControlPlane ve Worker için multi-stage Dockerfile eklendi; compose ortamı Kafka KRaft, kafka-ui, Postgres, kafka-init, controlplane ve worker servisleriyle çalışır hale getirildi. `scripts/smoke.sh` run başlatıp `Completed` durumunu poll ediyor; Kafka topic partitionları ve iki DB outbox lag'i doğrulandı.
- **Yarım kalan / sonraki oturuma devir:** Faz 1 kapanış tag'i henüz atılmadı; bu ayrı kapanış maddesi olarak duruyor.
- **Bir sonraki oturumun yapması gereken İLK şey:** Önce `.ai/PROGRESS.md` ve bu session dosyasını oku; ardından Faz 1 kapanış kriterlerini ve `v0.1` tag adımını ele al.
