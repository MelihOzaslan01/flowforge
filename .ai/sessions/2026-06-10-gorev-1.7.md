# Session Şablonu — dosya adı: YYYY-MM-DD-gorev-X.Y.md

## Oturum Bilgisi
- **Tarih:** 2026-06-10
- **Ajan:** Codex
- **Hedef görev(ler):** 1.7 — ControlPlane projeksiyon consumer'ı

## İnsanın Verdiği Görev Tanımı
Önce .ai/PROGRESS.md'yi ve son session dosyasını oku. SADECE Görev 1.7: ControlPlane'e ikinci BackgroundService — flowforge.job.events'i AYRI consumer group (controlplane-projection) ile dinler; JobRunCompleted → job_runs.Status=Completed + finished_at; JobRunFailed → Status=Failed + failed_step. Doğal idempotent: status zaten terminal ise atla; inbox tablosu KULLANMA. Diğer event tiplerini sadece offset commit'leyip geç. EnableAutoCommit=false burada da geçerli. Doğrulama + takip + commit no

## Oturum Sonu Özeti (ajan doldurur)
- **Tamamlanan:** ControlPlane'e `JobRunProjectionConsumer` hosted service'i eklendi. Consumer `flowforge.job.events` topic'ini `controlplane-projection` group ile dinliyor, `JobRunCompleted` ve `JobRunFailed` eventlerini terminal status idempotency kontrolüyle `job_runs` tablosuna yansıtıyor.
- **Yarım kalan / sonraki oturuma devir:** Faz 1.8 compose + smoke henüz başlamadı.
- **Bir sonraki oturumun yapması gereken İLK şey:** Önce `.ai/PROGRESS.md` ve bu session dosyasını oku; ardından Görev 1.8 için docker-compose, kafka-init ve `scripts/smoke.sh` akışına geç.
