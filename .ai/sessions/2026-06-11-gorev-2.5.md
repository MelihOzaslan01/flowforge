# Session Şablonu — dosya adı: YYYY-MM-DD-gorev-X.Y.md

## Oturum Bilgisi
- **Tarih:** 2026-06-11
- **Ajan:** Codex
- **Hedef görev(ler):** 2.5 — Chaos flag + `monthly-sales-report-chaos` seed job'ı

## İnsanın Verdiği Görev Tanımı
"Önce .ai/PROGRESS.md'yi ve son session dosyasını oku. SADECE Görev 2.5: (1) StepExecutor, step config'indeki chaos_fail_rate'i (0–1) okur; her denemede Random.Shared.NextDouble() < rate ise ChaosException fırlatır — retry mekanizması bunu normal hata gibi yaşar. (2) Seed'e ikinci job: monthly-sales-report-chaos — aynı 4 adım, GenerateReport'ta chaos_fail_rate=0.3, ayrıca hızlı test için adım sürelerini yarıya indir (config üzerinden, ör. duration_ms). Mevcut job'a DOKUNMA. (3) Idempotent seed korunur. Doğrulama BU SEFER CANLI: compose up --build → chaos job'ı 5+ kez tetikle → en az bir run'da Failed bekle → o run için: dlq topic'inde mesaj VAR, job_step_runs'ta Compensated satırlar ters sırada VAR, job_runs.status=Failed, failed_step dolu. Hepsi smoke benzeri bir scripts/chaos-smoke.sh ile otomatlanabilir ama elle doğrulama da kabul. Takip + commit no (2.3/2.4 dahil tüm bekleyenler commit'li olmalı)."

## Oturum Sonu Özeti (ajan doldurur)
- **Tamamlanan:** `StepExecutor` artık `duration_ms` ve `chaos_fail_rate` config değerlerini okuyor; chaos tetiklenirse `ChaosException` fırlatıyor. `monthly-sales-report-chaos` seed job'ı idempotent eklendi, mevcut `monthly-sales-report` seed davranışı korunuyor.
- **Tamamlanan:** `scripts/chaos-smoke.sh` eklendi. Script ControlPlane health bekler, chaos job'ı en az 5 kez tetikler, Failed run bulunca `job_runs`, `worker_db.job_step_runs` compensation sırası ve Kafka DLQ topic içeriğini doğrular.
- **Yarım kalan / sonraki oturuma devir:** Canlı doğrulama yapılamadı; Docker Desktop daemon/pipe görünmüyor. `docker compose up -d --build` hem normal hem izinli modda `open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified` hatası verdi. Docker Desktop açıldıktan sonra ilk iş `docker compose up -d --build` ve `scripts/chaos-smoke.sh` çalıştırılmalı.
- **Bir sonraki oturumun yapması gereken İLK şey:** Docker Desktop'ın çalıştığını doğrula; ardından 2.5 canlı smoke'u koş ve yeşilse BACKLOG'da 2.5'i `[x]` yapıp PROGRESS'e takip düzeltmesi ekle.
