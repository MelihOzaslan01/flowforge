# Session Sablonu - dosya adi: YYYY-MM-DD-gorev-X.Y.md

## Oturum Bilgisi
- **Tarih:** 2026-06-12
- **Ajan:** Codex
- **Hedef gorev(ler):** 5.1 - GitHub Actions CI

## Insanin Verdigi Gorev Tanimi
"Once .ai/PROGRESS.md'yi ve son session dosyasini oku. SADECE Gorev 5.1: .github/workflows/ci.yml. (1) build-and-unit job'i: push + PR tetiklemeli; ubuntu-latest; dotnet 9 setup; dotnet build -warnaserror; unit testler (tests/FlowForge.UnitTests). (2) integration job'i: schedule (nightly, cron '0 2 * * *') + workflow_dispatch (elle tetikleme — ilk dogrulama icin sart); Testcontainers ubuntu-latest'ta Docker'la calisir, ekstra servis tanimi GEREKMEZ; timeout-minutes: 20. (3) Cache: actions/cache ile NuGet (~/.nuget/packages, key: csproj hash). (4) README'ye CI badge satiri (build-and-unit workflow'u icin). (5) Dogrulama: YAML lint + push sonrasi Actions sekmesinde build-and-unit yesil; integration workflow_dispatch ile elle tetiklenip sonucu raporlanir — CI'da Kafka container startup'i lokalden yavastir, timeout patlarsa test fixture'daki bekleme sureleri CI icin esnetilir (ortam degiskeniyle, koda sabit yazmadan). Takip"

## Oturum Sonu Ozeti (ajan doldurur)
- **Tamamlanan:** `.github/workflows/ci.yml` eklendi. Workflow adi `build-and-unit`; push/PR eventlerinde `build-and-unit` job'i, schedule/manual eventlerinde `integration` job'i kosacak sekilde ayrildi.
- **Tamamlanan:** Iki job da `ubuntu-latest`, `actions/setup-dotnet@v4` ile .NET 9, `actions/cache@v4` ile `~/.nuget/packages` cache ve `hashFiles('**/*.csproj')` key kullanir. Integration job `timeout-minutes: 20` ve ekstra service tanimi olmadan Testcontainers'a birakildi.
- **Tamamlanan:** README basina `ci.yml` push badge'i eklendi.
- **Dogrulama:** YAML lint, local build/unit, commit/push, Actions build-and-unit ve manual integration sonucu bu oturumda tamamlanacak.
- **Not/risk:** Integration job CI'da Kafka startup gecikmesine takilirsa fixture timeoutlari ortam degiskeni ile esnetilecek; bu ilk uygulamada koda sabit yeni timeout yazilmadi.
- **Yarim kalan / sonraki oturuma devir:** —
- **Bir sonraki oturumun yapmasi gereken ILK sey:** Once `.ai/PROGRESS.md` ve bu session dosyasini oku.
